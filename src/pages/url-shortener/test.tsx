import Sidebar from "../../components/Utils/Sidebar";

import

const UrlShortenerPage = () => {
    return (
        <div className="min-h-screen min-w-screen bg-semantic-muted">
            <Sidebar></Sidebar>

            <main className="min-w-screen min-h-screen p-6">
                <header>

                </header>
            </main>
        </div>
    );
}
 
export default UrlShortenerPage;